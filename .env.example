# Copy to .env in project root — required for frontend

# API (Vite proxies /api and /ws to backend in dev)
VITE_API_URL=/api/v1

# Must be true for production — all data from PostgreSQL via API
VITE_USE_API=true
